import { ShoppingBag, User, ShieldCheck, Heart } from 'lucide-react';
import { Link, NavLink } from 'react-router-dom';
import { useAuth } from '../store/authStore';
export default function Navbar(){ const {user,logout}=useAuth(); const nav=['/products','/cart','/orders']; return <header className="sticky top-0 z-50 bg-white/80 backdrop-blur border-b border-slate-200"><div className="container h-16 flex items-center justify-between"><Link to="/" className="font-black text-2xl tracking-tight">ShopSphere</Link><nav className="hidden md:flex items-center gap-5 text-sm font-semibold">{nav.map(n=><NavLink key={n} to={n} className={({isActive})=>isActive?'text-slate-950':'text-slate-500'}>{n.slice(1).toUpperCase()}</NavLink>)}{user?.role==='admin'&&<Link to="/admin" className="flex gap-1"><ShieldCheck size={16}/>ADMIN</Link>}</nav><div className="flex items-center gap-3"><Link to="/wishlist"><Heart size={20}/></Link><Link to="/cart"><ShoppingBag size={20}/></Link>{user?<button onClick={logout} className="btn-ghost py-2">Logout</button>:<Link to="/login" className="btn-primary py-2"><User size={16}/> Login</Link>}</div></div></header> }
