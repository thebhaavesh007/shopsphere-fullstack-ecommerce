import { motion } from 'framer-motion';
import { Link } from 'react-router-dom';
import { useEffect, useState } from 'react';
import { api } from '../services/api';
import ProductCard from '../components/ProductCard';
import { Product } from '../types';
export default function Home(){ const [products,setProducts]=useState<Product[]>([]); useEffect(()=>{api.get('/products?limit=8').then(r=>setProducts(r.data.products))},[]); return <main><section className="container py-16 grid lg:grid-cols-2 gap-10 items-center"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}}><p className="font-bold text-slate-500">NEW SEASON COLLECTION</p><h1 className="text-5xl md:text-7xl font-black tracking-tight mt-3">Premium shopping, built for speed.</h1><p className="text-slate-600 text-lg mt-5">Browse curated products, pay securely, track orders, and manage everything through a modern admin dashboard.</p><div className="flex gap-3 mt-8"><Link to="/products" className="btn-primary">Start Shopping</Link><Link to="/admin" className="btn-ghost">Admin Demo</Link></div></motion.div><div className="card p-4 bg-gradient-to-br from-slate-900 to-slate-700"><img className="rounded-3xl h-[420px] w-full object-cover opacity-90" src="https://images.unsplash.com/photo-1441986300917-64674bd600d8?q=80&w=1200&auto=format&fit=crop"/></div></section><section className="container"><div className="flex items-end justify-between mb-6"><div><h2 className="text-3xl font-black">Featured Products</h2><p className="text-slate-500">Fast, responsive product grid with real API data.</p></div><Link to="/products" className="font-bold">View all</Link></div><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">{products.map(p=><ProductCard key={p._id} p={p}/>)}</div></section><section className="container mt-16 grid md:grid-cols-3 gap-5">{['Secure Payments','Fast Delivery','Admin Analytics'].map(t=><div className="card p-7" key={t}><h3 className="font-black text-xl">{t}</h3><p className="text-slate-500 mt-2">Production-focused workflow with authentication, checkout, tracking, and dashboards.</p></div>)}</section></main> }
